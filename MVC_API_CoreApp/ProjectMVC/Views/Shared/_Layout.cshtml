<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="icon" type="image/x-icon" href="/images/icon.png">
    
    <style>
        html, body {
            height: 100%;
            margin: 0;
            background-color: #0f0f0f;
            color: #dddddd;
            font-family: 'Courier New', monospace;
        }

        .wrapper {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1;
        }

        footer {
            background-color: #1a1a1a;
            color: #ffffff;
            text-align: center;
            padding: 10px 0;
        }
        
        footer .container {
            color: #ffffff;
        }

        .navbar {
            background-color: #1a1a1a !important;
            border-bottom: 1px solid #333;
        }

        .nav-link, .navbar-brand {
            color: #ccc !important;
        }

        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
            text-shadow: 0 0 5px #fff;
        }
        
        form .form-inline{
                background-color: #1a1a1a;
                display: inline-block;
                border-radius: 8px;
                border: 2px solid #333;
                box-shadow: 0 0 10px #444;
                text-align: left;
                max-width: 400px;
                margin-left: auto;
                margin-right: auto;
        }
        
.logout-button {
    background: none;
    border: none;
    color: #fff;               /* Adjust to match navbar text */
    height: 100%;              /* Take full height of parent */
    font-size: 0.875rem;       /* Smaller font size */
    line-height: 2.5rem;       /* Match typical navbar item height */
    cursor: pointer;
    text-decoration: none;     /* Remove underline for cleaner look */
    display: flex;
    align-items: center;       /* Vertically center text */
    font-weight: 500;
    transition: color 0.3s ease;
}

.logout-button:hover,
.logout-button:focus {
    color: #ff4444;
    outline: none;
}

    </style>
</head>
<body>
<div class="wrapper">
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">Leaderboard Manager</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                    </ul>

                    @if (User.Identity is { IsAuthenticated: true })
                    {
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <form method="post" asp-area="" asp-controller="Account" asp-action="Logout" class="form-inline">
                                    <button type="submit" class="logout-button nav-link">Logout</button>
                                </form>
                            </li>
                        </ul>
                    }
                </div>

            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted mt-auto">
        <div class="container">
            Created by Vlasenko Zahar 2025
        </div>
    </footer>
</div>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>
